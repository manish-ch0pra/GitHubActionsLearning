name: main.yml

on:
  push:
  schedule:
    - cron: "*/5 * * * *" 

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: run ps script
        shell: pwsh
        run: .\HelloWorld.ps1
        
